# Pinned per section 7 of the spec. There is no requirements.in and no
# compile step: this file is the source of truth and every version here was
# pinned by hand. To upgrade one, change it here and re-run the test suite and
# the eval, because a silent retrieval regression will not fail a unit test.

# --- Web framework ---
fastapi==0.118.0
uvicorn[standard]==0.37.0
pydantic==2.11.9
pydantic-settings==2.11.0
slowapi==0.1.9

# --- LLM ---
anthropic==0.122.0

# --- Vector store and embeddings ---
chromadb==1.1.0
# sentence-transformers is intentionally NOT used: the local embedding path
# runs all-MiniLM-L6-v2 through the onnxruntime model bundled with Chroma,
# which is a ~90MB download with no PyTorch dependency (~2GB).
voyageai==0.3.4

# --- PDF handling ---
pypdf==6.1.1
pdfplumber==0.11.7
pymupdf==1.26.4

# --- Corpus generation (dev-only, used by tools/generate_corpus.py) ---
reportlab==4.4.4

# --- Security tooling ---
# Runs in the pre-commit hook (.pre-commit-config.yaml) and over the tracked
# file set before a push. Section 7 of the spec asks for a scanner rather than
# a promise.
detect-secrets==1.5.0

# --- Testing and typing ---
pytest==8.4.2
mypy==1.18.2
httpx==0.28.1
