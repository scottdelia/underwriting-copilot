# Loaded by `vite build --mode demo` (npm run build:demo).
# Makes the bundle answer from frontend/public/fixtures instead of a live
# backend. See src/api/client.ts for why the published build works this way.
VITE_DEMO_MODE=true
